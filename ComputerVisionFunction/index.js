module.exports = function (context, imageProcessingJob) {
    context.log('JavaScript queue trigger function processed work item', myQueueItem);
    context.done();
    
    // call to computer vision
};